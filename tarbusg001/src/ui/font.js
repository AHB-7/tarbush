import { Bellota, Roboto } from "next/font/google";

export const roboto = Roboto({
    subsets: ["latin"],
    display: "swap",
    weight: "400",
});
export const bellota = Bellota({
    weight: "400",
    display: "swap",
    subsets: ["latin"],
});
